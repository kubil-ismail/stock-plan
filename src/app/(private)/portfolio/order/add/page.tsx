import AddOrder from "@/views/(private)/portfolio/add/Add.views";
import React from "react";

interface Props {}

function Page(props: Props) {
  const {} = props;

  return <AddOrder />;
}

export default Page;
